<%= provide(:pagetitle, "Edit Payment") %>
<div class="well">
  <%= form_for(@payment) do |f| %>
    <%= render 'fields', :f => f %>
    <%= f.submit "Update", :class => "btn btn-primary" %>
  <% end %>
</div>
