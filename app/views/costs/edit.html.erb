<%= provide(:pagetitle, "Edit Cost")%>
<div class="well">
  <%= form_for(@cost) do |f| %>
    <%= render 'fields', :f => f %>
    <%= f.submit "Update", :class=> "btn btn-primary" %>
  <% end %>
</div>
